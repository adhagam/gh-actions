name: First Workflow

on: [push]

jobs:
  run-shell-command:
    runs-on: ubuntu-latest

    steps:
      - name: echo shell command
        run: echo 'First Work flow'
      - name: RUn multi line command
        run: |
          node -v
          npm -v